# Common command hooks — comment/uncomment template
# docs/features/hook.md
# Requires [klipper_extras]. No G-code command.
#
# Wraps each action feature command. Per-action hooks are on that feature.

[klipper_extras hook]
#command_before_gcode:
#  M117 common before
#command_after_gcode:
#  M117 common after
#on_hook_fail: stop
#debug: False
